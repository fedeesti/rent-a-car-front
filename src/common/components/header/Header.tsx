import Navbar from '../nabvar/Navbar';
import SideBar from '../side/SideBar';

function Header() {
  return (
    <header>
      <Navbar />
      <SideBar />
    </header>
  );
}

export default Header;
